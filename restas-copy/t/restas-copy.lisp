(in-package :cl-user)
(defpackage restas-copy-test
  (:use :cl
        :restas-copy
        :prove))
(in-package :restas-copy-test)

;; NOTE: To run this test file, execute `(asdf:test-system :restas-copy)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
